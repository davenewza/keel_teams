import { Myfunc } from '@teamkeel/sdk';

// To learn more about what you can do with custom functions, visit https://docs.keel.so/functions
export default Myfunc(async (ctx, inputs) => {
    return {};
});